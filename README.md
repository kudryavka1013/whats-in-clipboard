# What's in clipboard

Press `Ctrl + V` (Windows/Linux) or `Cmd + V` (Mac) to view the clipboard contents.

## How It Works

All data is retrieved using `e.clipboardData` and `navigator.clipboard.read()`
